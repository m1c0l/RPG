#include "Type.h"
#include "Random.h"

#include <limits.h>

using namespace std;

string getRandValue(SupportedType type) {
	string randString;
	switch(type) {
		case CHAR:
		case INT:
		case LONG:
		case LLONG:
			break;
	}
	return "";
}